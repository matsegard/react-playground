import React, { CSSProperties } from "react";
import SectionCard from "../sectionCard";

function DetailPage() {
  return <div style={rootStyle}>HELLO WORLD</div>;
}

const rootStyle: CSSProperties = {
  flex: 1,
  background: "#444",
};

export default DetailPage;
